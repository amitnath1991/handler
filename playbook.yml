- hosts: all
  become: true
  tasks:
   - copy:
        content: "hello world"
        dest: /tmp/amit.txt
     notify: restart apache

  handlers:
    name: restart apache
    service:
      name: httpd
      state: restarted
      enabled: yes
           
